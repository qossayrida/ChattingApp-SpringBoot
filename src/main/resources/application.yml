spring:

  application:
    name:
      websocket

  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      group-id: myGroup


  data:
    mongodb:
      username: chatapp
      password: chatapp
      host: localhost
      port: 27018
      database: chat_app
      authentication-database: admin

server:
  port: 8088